"""
The Request/Response Cycle → El navegador usa el protocolo HTTP para solicitar y recibir datos de un servidor.

A blank line → Los encabezados HTTP están separados del cuerpo por una línea en blanco.

import socket → Antes de usar sockets en Python, se debe importar el módulo socket.

server → El servidor debe estar activo antes de que el cliente intente conectarse.

An "in-progress" phone conversation → Una conexión de socket es similar a una conversación en curso.

HyperText → HTTP significa HyperText Transfer Protocol.

Which application talks first? The client or server? → Un protocolo de capa de aplicación define cómo interactúan el cliente y el servidor.

Protocol, host, and document → En http://www.dr-chuck.com/page1.htm, el protocolo es http, el host es www.dr-chuck.com, y el documento es page1.htm.

GET → Un clic en un enlace <a href="page1.htm"> genera una solicitud HTTP GET.

IETF → El Internet Engineering Task Force (IETF) es la organización que publica estándares para protocolos de Internet.
"""
